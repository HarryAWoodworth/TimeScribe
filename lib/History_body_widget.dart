import 'package:flutter/material.dart';

class HistoryBodyWidget extends StatelessWidget {
  final Color color;

  HistoryBodyWidget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}